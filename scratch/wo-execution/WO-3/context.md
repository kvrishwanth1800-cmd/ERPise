# WO-3 context

Source requirement: REQ-TEN-001. The implementation follows the Tenant and Organization feature blueprint and Tenant and Organization Context component blueprint. It uses server-resolved `ScopeContext`, tenant ownership on each hierarchy record, and an in-memory outbox boundary for `OrganizationChanged` events.
